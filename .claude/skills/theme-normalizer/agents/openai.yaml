interface:
  display_name: "Theme Normalizer"
  short_description: "Normalize hard-coded MDX colors to deck theme CSS variables"
  default_prompt: "Use $theme-normalizer to dry-run color normalization for this deck first."
